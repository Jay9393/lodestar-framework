# Gemini Instructions — {{PROJECT_NAME}}

This project follows the **Lodestar framework**.

Before doing anything, read `PROJECT.md` (stage, mode, north star) and
`.lodestar/SPEC.md` (full framework). The shared agent instructions live in
`AGENTS.md` — follow them.

Lifecycle: `1-discovery → 2-design → 3-build → 4-operate`. The stage is the
directory. Keep the north star in view and never skip a stage without logging a
reason in `docs/99-decision-research/gate-skips/`.
